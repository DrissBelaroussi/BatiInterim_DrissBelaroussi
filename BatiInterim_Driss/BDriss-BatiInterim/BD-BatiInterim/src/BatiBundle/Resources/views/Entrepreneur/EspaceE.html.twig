{# empty Twig template #}
<!DOCTYPE html>
<html>
    <head>
        <title>Bati-Interim</title>

        {% stylesheets '@BatiBundle/Resources/public/css/*' %}
        <link rel="stylesheet" href="{{ asset_url }}" type="text/css" />
        {% endstylesheets %} 
    </head>
    <body>
        
        {{ include("BatiBundle:Template:ConnectedMenuE.html.twig") }}
        {%if entrepreneur is defined %}
            {{ entrepreneur.nomsociete}}
            {{entrepreneur.ville}} 
        {% endif %}


       <div class="container">
            <h2>Espace Entrepreneur</h2>
            <p>Vous êtes sur la plateforme dédiée aux entrepreneurs.</p>
            <p class="lead"> Si vous n'êtes pas autorisé à accéder à cette plateforme</p>
            <p>Veuillez-vous déconnecter.</p>
        </div>


        {#<table class='tableBoutons'>
            <tr>
                <td>
                    <form class ="formConnexion" action="{{ path('GererArtisans') }}" method="post">
                        <input  type="submit" class="btn btn-inverse" value="Gérer les artisans" />
                    </form>  

                </td>
                <td>
                    <form class ="formConnexion" action="{{ path('CreerChantier') }}" method="post">
                        <input  type="submit"  class="btn btn-inverse" value="Gérer les chantiers" />
                    </form>

                </td>
                <td>
                    <form class ="formConnexion" action="{{ path('ConsulterAffectations') }}" method="post">
                        <input  type="submit"  class="btn btn-inverse" value="Consulter les Affectations" />
                    </form>

                </td>
                
                <td>
                    <form class ="formConnexion" action="{{ path('CreerChefChantier') }}" method="post">
                        <input  type="submit"  class="btn btn-inverse" value="Renseigner Chef de chantier" />
                    </form>

                </td>
            </tr>#}



        </table>







       {# <form class ="formConnexion" action="{{ path('Deconnexion') }}" method="post">
            <input  type="submit"  class="btn btn-default" value="Déconnexion" />
        </form>#}



    </body>
    
    <footer>
    {{ include("BatiBundle:Template:Footer.html.twig") }}
</footer>
